# Python hooks: Ruff only (lint + format) — run on staged files only
repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: bash -c 'uv run ruff check --fix "$@"'
        language: system
        types: [python]
        pass_filenames: true

      - id: ruff-format
        name: ruff format
        entry: bash -c 'uv run ruff format "$@"'
        language: system
        types: [python]
        pass_filenames: true
