{
  "secrets": [
    {
      "key": "apiKey",
      "envVar": "REIMBURSEMENT_AUTOMATION_API_KEY",
      "description": "Upstream API key or shared credential (replace via GitHub Actions)",
      "required": false
    }
  ]
}
