/**
 * Environment variable names that may be supplied in GitHub Actions
 * to populate Secrets Manager (see configs/secrets).
 */
export enum EnvVars {
  ReimbursementAutomationApiKey = 'REIMBURSEMENT_AUTOMATION_API_KEY',
}
